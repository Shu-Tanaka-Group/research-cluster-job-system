apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dispatcher
  namespace: cjob-system
spec:
  selector:
    matchLabels:
      app: dispatcher
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
